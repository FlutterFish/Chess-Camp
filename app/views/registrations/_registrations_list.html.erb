  <div class="row">

  <div class= "col s12">
  <p id="notice"><%= notice %></p>
  <h3><%= state %> Registrations</h3>

  <table>
    <thead>
      <tr>
        <th>Camp Name</th>
        <th>Student Name</th>
        <th>Start Date</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% registrations.each do |reg| %>
        <tr>
          <td><%= link_to reg.camp.name, camp_path(reg.camp) %></td>
          <td><%= link_to reg.student.name, student_path(reg.student) %></td>
          <td><%= reg.camp.start_date %></td>
          <td>
            <%= link_to(eval("registration_path(reg)"), method: 'delete', data: {confirm: "Are you sure you want to permanently delete this registration?"}, class: 'waves-effect waves-light btn btn-alert') do %>
                &nbsp;<%= "Remove" %>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <span class="paginate">
    <%= will_paginate registrations, :previous_label => "Previous&nbsp;", :next_label => "&nbsp;Next" %>
  </span>